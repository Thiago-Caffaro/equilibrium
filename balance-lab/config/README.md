# Local mod configuration overrides

Place only a scoped override for the mod that owns the changed behavior here.
Before adding a key, inspect comments/schema from the exact installed JAR and
record the required reload or restart in the inventory. Never use a config
change as an undocumented global balance adjustment.

Rollback restores or removes the one copied config file/key; no Core source or
JAR change is part of this path.
