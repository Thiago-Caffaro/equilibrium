# Datapack and tag experiments

Use this directory for data that should remain a standalone datapack rather
than a KubeJS script. Prefer additive, shared `c:` tags when the intent is a
material category, and verify the tag contents in the loaded test pack.

Record whether `/reload` applied the experiment and keep the rollback to a
single copied datapack directory or ZIP.
