# Restart-only KubeJS experiments

Use this directory only for registration-time changes that the installed KubeJS
documentation identifies as startup-only. Each experiment requires a full
instance restart and an explicit inventory entry explaining why
`server_scripts` is insufficient.

Do not use startup scripts merely to avoid modeling a reloadable recipe or tag
change as server data.
